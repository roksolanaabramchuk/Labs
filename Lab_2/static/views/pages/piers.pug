extends ../layout.pug

block content
    h2 Колекція пірсів
    br
    br
    each val, index in data
        div.piers= 'Пірс '+ val.name + ' з номером ' + val.number + ' та місткістю:' + val.Capacity
