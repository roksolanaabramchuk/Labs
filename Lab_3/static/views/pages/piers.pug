extends ../layout.pug

block content
    h2 Колекція пірсів
    br

    br
    each val, index in data
        div.piers= 'Пристань '+ val.name + ' з номером ' + val.number + ' та місткістю: ' + val.Capacity + '. осандка: ' + val.minOsanka
            p='Кораблів на пристані - ' + val.ships.length
            form(method='POST' action=`/addShipToPiers/${val.number}`)
                label(for='OurShipEdit') Our Ship for piers:
                    select#OurShipEdit(name = 'index')
                        each val, index in ships
                            option='Номер: ' + val.number + '. Імя: ' + val.name +  '. Країна: ' + val.country + '. Осадка: ' + val.osanka
                br
                button.btn.btn-primary(type='submit') Add
